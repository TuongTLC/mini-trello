export interface userRegisterModel  {
    name: string;
    email: string;
    position: string;
}
export interface userAuthModel {
    email: string;
    otp: string;
}
export interface userInfoModel {
    id: string;
    name: string;
    email: string;
    position: string;
}